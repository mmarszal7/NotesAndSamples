<Window x:Class="HtmlToXAML.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HtmlToXAML"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="700" MinHeight="450" MinWidth="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Black" BorderThickness="2" Grid.Row="0" Margin="20" Padding="5">
            <TextBlock x:Name="Text" Text="{Binding Test}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextWrapping="Wrap"/>
        </Border>
        
        <Button Content="Convert to Plain Text" Margin="10" Command="{Binding HtmlConverter}" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        <Button Content="Convert back to HTML" Margin="10" Command="{Binding UnConverter}" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Right"/>

    </Grid>
</Window>
