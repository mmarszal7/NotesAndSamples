<p>Hello I´m developing regex regular expressions with Java but this dont works..</p>\n\n<p>This is my main code:</p>\n\n<p>This is the patterns for regular expressions:</p>\n\n<pre><code>String pattern = \"(?m)(?&lt;=\\bNome:).*$\";\n        String secondpattern = \"(?m)(?&lt;=\\bEmail:).*$\";\n        String thirdpattern = \"(?m)(?&lt;=\\bFacebookID:).*$\";\n        String fourthpattern = \"(?m)(?&lt;=\\bMorada:).*$\";\n</code></pre>\n\n<p>...............</p>\n\n<pre><code>    // Matcher the pattern\n    Pattern nme = Pattern.compile(pattern);\n    Pattern ema = Pattern.compile(secondpattern);\n    Pattern fb = Pattern.compile(secondpattern);\n    Pattern mor = Pattern.compile(secondpattern);\n\n    BufferedReader reader = new BufferedReader(new FileReader(fileNamePath));\n    String line;\n    line = reader.readLine();\n\n    User user = new User();\n\n    while (line != null) {\n\n        Matcher m = nme.matcher(line);\n\n        if (m.matches()) {\n\n            user.setName(m.group());\n\n            line = reader.readLine();\n\n        }\n        Matcher m1 = ema.matcher(line);\n        if (m1.matches()) {\n\n            user.setEmail(m1.group());\n            line = reader.readLine();\n        }\n        Matcher m2 = fb.matcher(line);\n        if (m2.matches()) {\n            user.setFacebookUrl(m2.group());\n            line = reader.readLine();\n        }\n        Matcher m3 = mor.matcher(line);\n        if (m3.matches()) {\n\n            // if (mas.lookingAt()) { // Check if exist datetime;\n            user.setAdress(m3.group());\n\n            line = reader.readLine();\n\n        }\n\n        // /System.out.println(line);\n        UserList.add(user);\n        line = reader.readLine();\n\n    }\n</code></pre>\n\n<p>........</p>\n\n<p>In the loop of List of objects the name is printing:</p>\n\n<pre><code>Namenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\nNamenull\n</code></pre>\n"